---
- name: date
  hosts: all

  tasks:

  - name: date
    shell: kubectl run test --rm --requests="cpu=500m,memory=512Mi" --limits="cpu=500m,memory=512Mi" -n sandbox --restart=Never -it --image=alpine date
    register: out

  - debug: var=out.stdout_lines
